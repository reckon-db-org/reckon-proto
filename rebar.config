%% Minimal rebar.config — reckon-proto is proto-only, no Erlang
%% source. This file exists so rebar3 consumers can fetch the repo
%% as a dep without "no rebar.config found" warnings.

{erl_opts, []}.
{deps, []}.
